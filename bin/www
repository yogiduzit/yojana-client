const app = require('../app');
const IP = process.env.IP  || '0.0.0.0';
const PORT = process.env.PORT || 8080;

app.listen(PORT, IP, function() {
 console.log('Application running at ' + IP + ':' + PORT);
});